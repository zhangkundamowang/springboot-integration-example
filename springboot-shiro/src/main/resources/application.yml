spring:
  #ʹ��Thymeleafģ������
  thymeleaf:
    mode: HTML5
    encoding: UTF-8
    cache: false  #ʹ��Thymeleafģ�����棬�رջ���
    servlet:
      content-type: text/html
  # JPA���ݿ�������Ϣ
  datasource:
    url: jdbc:mysql://localhost:3306/springboot-integration-example?useSSL=false&amp
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    database: MYSQL
    show-sql: true
    open-in-view: true
    properties:
      hibernate:
        enable_lazy_load_no_trans: true #ʹ����ʱ����ʱ����Session����������
        dialect: org.hibernate.dialect.MySQL5Dialect
        ddl-auto: update
server:
  port: 8091
